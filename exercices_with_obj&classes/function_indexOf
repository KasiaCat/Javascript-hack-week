var free_rooms = [2, 5, 6, 10];
var requested_room = prompt();

showIsRoomFree(requested_room);

function showIsRoomFree(room_number){
    if (free_rooms.indexOf(room_number))
        console.log("Pokój wolny.");
        else
        console.log("Pokój zajęty.");
}